name: Build and Push Docker Images

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}
    
    - name: Extract metadata for versioning
      id: meta
      run: |
        echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
        echo "date=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
    
    - name: Build and push Server Image
      uses: docker/build-push-action@v5
      with:
        context: ./server
        file: ./server/Dockerfile
        push: true
        tags: |
          kanhaiya25/cinepass-server:latest
          kanhaiya25/cinepass-server:${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}
          kanhaiya25/cinepass-server:dev
        cache-from: type=registry,ref=kanhaiya25/cinepass-server:latest
        cache-to: type=inline
    
    - name: Build and push Client Image
      uses: docker/build-push-action@v5
      with:
        context: ./client
        file: ./client/Dockerfile
        push: true
        tags: |
          kanhaiya25/cinepass-client:latest
          kanhaiya25/cinepass-client:dev
          kanhaiya25/cinepass-client:${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}
        cache-from: type=registry,ref=kanhaiya25/cinepass-client:latest
        cache-to: type=inline